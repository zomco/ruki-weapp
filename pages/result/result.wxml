<view class="page">
  <view class="page__bd">
    <view class="weui-panel weui-panel_access">
      <view class="weui-panel__hd">“{{chars}}” 的视频</view>
      <view class="weui-panel__bd">
        <view 
          class="weui-media-box weui-media-box_text"
          wx:for="{{resultArray}}"
          wx:key="{{item.id}}"
        >
          <view class="weui-media-box__title weui-media-box__title_in-text">
            <navigator
              class="item"
              url="/pages/detail/detail?id={{item.id}}"
            >
              <image
                class="screenshot"
                src="{{item.posterFile}}" 
              />
              <view class="short" wx:if="{{item.isCrash && !item.isRule}}">事故</view>
              <view class="short" wx:elif="{{!item.isCrash && item.isRule}}">违章</view>
              <view class="short" wx:elif="{{item.isCrash && item.isRule}}">违章·事故</view>
            </navigator>
          </view>
          <view class="weui-media-box__desc">由各种物质组成的巨型球状天体，叫做星球。星球有一定的形状，有自己的运行轨道。</view>
          <view class="weui-media-box__info">
            <view class="weui-media-box__info__meta">{{item.when}}</view>
            <view class="weui-media-box__info__meta">{{item.where}}</view>
          </view>
        </view>
        <block wx:if="{{resultArray.length === 0}}">
          <view class="weui-loadmore weui-loadmore_line">
            <view class="weui-loadmore__tips weui-loadmore__tips_in-line">暂无数据</view>
          </view>
        </block>
        <block wx:if="{{resultArray.length > 0}}">
          <view class="weui-loadmore weui-loadmore_line">
            <view class="weui-loadmore__tips weui-loadmore__tips_in-line weui-loadmore__tips_in-dot"></view>
          </view>
        </block>
      </view>
    </view>
  </view>
  <view class="page_ft">
    <view class="weui-footer weui-footer_fixed-bottom">
      <view class="weui-footer__text">© 2017 ruki.pw</view>
    </view>
  </view> 
</view>